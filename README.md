# The LoCo Code

GitHub Pages static site for `lococode.net`.

Architecture:
- `lococode.net` = campaign and product shortcut for The LoCo Code.
- `SignalTowerPress.com` = publisher authority page.
- `RonDashStella.com` = store, checkout, Field Kit, Companion Field Course, audits, and managed services.

Public language rules:
- Use: local visibility repair, owner-authorized audit, public presence cleanup, managed local presence.
- Do not use: scraping Google, profile takeover, ranking guarantee, Google loophole, automation exploit.

Deployment:
- GitHub Pages source: `main` / root.
- Custom domain: `lococode.net`.
